/**
 * ============================================
 * 骑士实体 — 当前唯一角色
 * ============================================
 * 职责：骑士的外观、动画状态管理
 *
 * 动画状态映射（对应 序列帧/人物动作/ 下的子目录）：
 *  待机 → 待机动作
 *  移动 → 移动位置_色键输出
 *  普攻 → 挥砍前刺_色键输出
 *  技能攻击 → 竖劈_色键输出
 *  防御 → 防御_色键输出
 *  受击 → 受击_色键输出
 *  死亡 → 死亡动作_色键输出
 *  治疗 → 治疗_色键输出
 *  悬浮 → 悬浮_色键输出
 *  下坠 → 下坠_色键输出
 *  强化待机 → 强化状态待机动作_色键输出
 *  强化攻击 → 强化状态攻击动作_色键输出
 *  强化 → 强化状态_色键输出
 *
 * 扩展性：未来新增角色 → 在 实体/ 下新建文件，
 *         继承 Player 并绑定不同的动画资源即可
 */
export class Knight {
    constructor(player) {
        this.player = player;
        this.currentAnimation = '待机';
    }

    /** 根据动作名称播放对应的序列帧动画 */
    playAnimation(actionName) {
        // TODO: 从 游戏资源/序列帧/人物动作/ 加载对应帧
        this.currentAnimation = actionName;
    }

    /** 每帧更新动画 */
    update(deltaTime) {
        // TODO
    }
}
