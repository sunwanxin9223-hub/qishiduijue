/**
 * ============================================
 * 全局配置 — 所有可调数值集中管理
 * ============================================
 */

export const Config = {
    // ── 画布（与美术工具统一，1920×1080）──
    设计宽度: 1920,
    设计高度: 1080,

    // ── 基础数值 ──
    玩家血量: 2100,
    普攻伤害: 200,
    回血恢复量: 800,

    // ── 移动规则 ──
    基础移动身位: 1,
    神速移动身位: 2,

    // ── 平台跳跃掉血 ──
    跳跃掉血: {
        'V6→地面': 700,
        'V7→地面': 200,
        'V8→地面': 0,
    },

    // ── 对战规则 ──
    胜负方式: '一局定胜负',
    三局两胜目标胜场: 2,
    每回合时限秒: 30,

    // ── 网络 ──
    房间码长度: 6,
    房间超时秒: 60,
    WebSocket地址: '',
    排行榜积分每胜: 10,

    // ── 可选技能数 ──
    可选技能数量: 3,

    // ── 设置默认值 ──
    默认音效开关: true,
    默认音效音量: 80,
    默认背景音乐开关: true,
    默认背景音乐音量: 80,
};
