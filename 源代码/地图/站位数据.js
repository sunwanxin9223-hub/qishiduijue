/**
 * ============================================
 * 站位数据 — 地图的"数据库"
 * ============================================
 * 所有坐标来自美术工具导出（1920×1080 画布）
 * 坐标系：原点=画布中心，X 右正，Y 下正
 * 渲染时转换：canvasX = toolX + 960, canvasY = toolY + 540
 */

// ── 站位坐标（美术工具坐标系，原点=画布中心）──
export const 站位坐标_工具系 = {
    V1: { x: -495, y:  229 },   // 最左侧地面
    V2: { x: -321, y:  229 },   // V8 正下方地面
    V3: { x:  -88, y:  229 },   // V7 正下方地面
    V4: { x:  150, y:  228 },   // V6 正下方地面
    V5: { x:  338, y: -207 },   // 石柱顶端（最高）
    V6: { x:  150, y: -194 },   // 右侧悬空平台（V4 正上方）
    V7: { x:  -88, y:  -75 },   // 中间悬空平台（V3 正上方）
    V8: { x: -321, y:   44 },   // 左侧悬空平台（V2 正上方）
    V9: { x:  486, y:  108 },   // 传送风阵（画面最右侧地面）
};

/** 转换为 Canvas 渲染坐标（原点=左上角） */
export function 站位画布坐标(站位id) {
    const pos = 站位坐标_工具系[站位id];
    return {
        x: pos.x + 960,
        y: pos.y + 540,
    };
}

// ── 可站立的位列表 ──
export const 站位列表 = ['V1', 'V2', 'V3', 'V4', 'V5', 'V6', 'V7', 'V8'];

// ── 邻接关系（双向边）──
export const 邻接关系 = {
    V1: ['V2', 'V8'],
    V2: ['V1', 'V3'],
    V3: ['V2', 'V4'],
    V4: ['V3', 'V9'],          // V4→V9 单向，由传送规则处理
    V5: ['V6'],
    V6: ['V5', 'V7'],
    V7: ['V6', 'V8'],
    V8: ['V7', 'V1'],
};

// ── 传送规则 ──
export const 传送规则 = {
    V9: {
        自动传送至: 'V5',
        可站立: false,
        可攻击: false,
        触发方式: '到达即触发',
    }
};

// ── 跳跃落地规则 ──
export const 跳跃规则 = {
    V6: { 落点: 'V4', 掉血: 700 },
    V7: { 落点: 'V3', 掉血: 200 },
    V8: { 落点: 'V2', 掉血: 0 },
};

// ── 开局站位 ──
export const 开局站位 = {
    玩家1: 'V2',
    玩家2: 'V4',
};
